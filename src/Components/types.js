/* @flow */

/* Body Component */
export type BodyProps = {
  columns: string,
  data: Object[],
  arrayOption: ?string[],
}

/* Title Component */
export type TitleProps = {
  title: ?string,
  subTitle: ?string,
}

/* Header Component */
export type HeaderProps = {
  headers: string[],
}
