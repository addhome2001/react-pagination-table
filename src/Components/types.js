/* @flow */

/* Body Component */
export type BodyProps = {
  columns: string,
  data: Object[],
  arrayOption: ?string[],
}

/* Title Component */
export type TitlesProps = {
  title: ?string,
  subTitle: ?string,
  className: string,
}

/* Header Component */
export type HeaderProps = {
  headers: string[],
  className: string,
}
